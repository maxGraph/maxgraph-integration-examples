{
  "name": "maxgraph-ts-example-built-with-vite",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --base ./",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "~4.4.11"
  }
}
